<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.kh.healthcare.challenger.ChallengerMapper">
    <select id="checkOpenAndCheckPost">
        SELECT
        C.PERFORMANCE_START
        , C.PERFORMANCE_END
        , (SELECT ENROLL_DATE
                FROM CHALLENGER_POST
                WHERE CHALLENGER_NO = #{vo.no}
                AND MEMBER_NO = #{userNo}
                ORDER BY ENROLL_DATE DESC
                FETCH FIRST 1 ROW ONLY) AS today
        FROM CHALLENGER C
        WHERE C.NO = #{vo.no}
    </select>


</mapper>